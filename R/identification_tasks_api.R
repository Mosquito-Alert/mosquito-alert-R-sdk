#' Mosquito Alert API
#'
#' Introducing API v1 for Mosquito Alert platform, a project desgined to facilitate citizen science initiatives and enable collaboration among scientists, public health officials, and environmental managers in the investigation and control of disease-carrying mosquitoes.
#'
#' The version of the OpenAPI document: v1
#' Contact: it@mosquitoalert.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title IdentificationTasks operations
#' @description IdentificationTasksApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  annotations_create  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#' var_annotation_request <- AnnotationRequest$new(AnnotationClassificationRequest$new(123, "definitely"), "best_photo_uuid_example", AnnotationCharacteristicsRequest$new("male", "is_blood_fed_example", "is_gravid_example"), AnnotationFeedbackRequest$new("public_note_example", "internal_note_example", "user_note_example"), "is_flagged_example", "is_decisive_example", ObservationFlagsRequest$new("is_favourite_example", "is_visible_example"), c("tags_example")) # AnnotationRequest | 
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$annotations_create(var_observation_uuid, var_annotation_requestdata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$annotations_create(var_observation_uuid, var_annotation_request)
#' dput(result)
#'
#'
#' ####################  annotations_list  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#' var_classification_confidence_label <- "classification_confidence_label_example" # character |  (Optional)
#' var_classification_confidence_max <- 3.4 # numeric |  (Optional)
#' var_classification_confidence_min <- 3.4 # numeric |  (Optional)
#' var_classification_taxon_ids <- c(123) # array[integer] |  (Optional)
#' var_created_at_after <- "created_at_after_example" # character | Created at (Optional)
#' var_created_at_before <- "created_at_before_example" # character | Created at (Optional)
#' var_is_decisive <- "is_decisive_example" # character |  (Optional)
#' var_is_favourite <- "is_favourite_example" # character |  (Optional)
#' var_is_flagged <- "is_flagged_example" # character |  (Optional)
#' var_order_by <- c("-created_at") # array[character] | Ordenado   (Optional)
#' var_page <- 56 # integer | A page number within the paginated result set. (Optional)
#' var_page_size <- 56 # integer | Number of results to return per page. (Optional)
#' var_type <- "type_example" # character |  (Optional)
#' var_updated_at_after <- "updated_at_after_example" # character | Updated at (Optional)
#' var_updated_at_before <- "updated_at_before_example" # character | Updated at (Optional)
#' var_user_ids <- c(123) # array[integer] |  (Optional)
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$annotations_list(var_observation_uuid, classification_confidence_label = var_classification_confidence_label, classification_confidence_max = var_classification_confidence_max, classification_confidence_min = var_classification_confidence_min, classification_taxon_ids = var_classification_taxon_ids, created_at_after = var_created_at_after, created_at_before = var_created_at_before, is_decisive = var_is_decisive, is_favourite = var_is_favourite, is_flagged = var_is_flagged, order_by = var_order_by, page = var_page, page_size = var_page_size, type = var_type, updated_at_after = var_updated_at_after, updated_at_before = var_updated_at_before, user_ids = var_user_idsdata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$annotations_list(var_observation_uuid, classification_confidence_label = var_classification_confidence_label, classification_confidence_max = var_classification_confidence_max, classification_confidence_min = var_classification_confidence_min, classification_taxon_ids = var_classification_taxon_ids, created_at_after = var_created_at_after, created_at_before = var_created_at_before, is_decisive = var_is_decisive, is_favourite = var_is_favourite, is_flagged = var_is_flagged, order_by = var_order_by, page = var_page, page_size = var_page_size, type = var_type, updated_at_after = var_updated_at_after, updated_at_before = var_updated_at_before, user_ids = var_user_ids)
#' dput(result)
#'
#'
#' ####################  annotations_list_mine  ####################
#'
#' library(MosquitoAlert)
#' var_classification_confidence_label <- "classification_confidence_label_example" # character |  (Optional)
#' var_classification_confidence_max <- 3.4 # numeric |  (Optional)
#' var_classification_confidence_min <- 3.4 # numeric |  (Optional)
#' var_classification_taxon_ids <- c(123) # array[integer] |  (Optional)
#' var_created_at_after <- "created_at_after_example" # character | Created at (Optional)
#' var_created_at_before <- "created_at_before_example" # character | Created at (Optional)
#' var_is_decisive <- "is_decisive_example" # character |  (Optional)
#' var_is_favourite <- "is_favourite_example" # character |  (Optional)
#' var_is_flagged <- "is_flagged_example" # character |  (Optional)
#' var_order_by <- c("-created_at") # array[character] | Ordenado   (Optional)
#' var_page <- 56 # integer | A page number within the paginated result set. (Optional)
#' var_page_size <- 56 # integer | Number of results to return per page. (Optional)
#' var_type <- "type_example" # character |  (Optional)
#' var_updated_at_after <- "updated_at_after_example" # character | Updated at (Optional)
#' var_updated_at_before <- "updated_at_before_example" # character | Updated at (Optional)
#' var_user_ids <- c(123) # array[integer] |  (Optional)
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$annotations_list_mine(classification_confidence_label = var_classification_confidence_label, classification_confidence_max = var_classification_confidence_max, classification_confidence_min = var_classification_confidence_min, classification_taxon_ids = var_classification_taxon_ids, created_at_after = var_created_at_after, created_at_before = var_created_at_before, is_decisive = var_is_decisive, is_favourite = var_is_favourite, is_flagged = var_is_flagged, order_by = var_order_by, page = var_page, page_size = var_page_size, type = var_type, updated_at_after = var_updated_at_after, updated_at_before = var_updated_at_before, user_ids = var_user_idsdata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$annotations_list_mine(classification_confidence_label = var_classification_confidence_label, classification_confidence_max = var_classification_confidence_max, classification_confidence_min = var_classification_confidence_min, classification_taxon_ids = var_classification_taxon_ids, created_at_after = var_created_at_after, created_at_before = var_created_at_before, is_decisive = var_is_decisive, is_favourite = var_is_favourite, is_flagged = var_is_flagged, order_by = var_order_by, page = var_page, page_size = var_page_size, type = var_type, updated_at_after = var_updated_at_after, updated_at_before = var_updated_at_before, user_ids = var_user_ids)
#' dput(result)
#'
#'
#' ####################  annotations_retrieve  ####################
#'
#' library(MosquitoAlert)
#' var_id <- 56 # integer | A unique integer value identifying this expert report annotation.
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$annotations_retrieve(var_id, var_observation_uuiddata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$annotations_retrieve(var_id, var_observation_uuid)
#' dput(result)
#'
#'
#' ####################  assign_next  ####################
#'
#' library(MosquitoAlert)
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$assign_next(data_file = "result.txt")
#' result <- api_instance$identification_tasks_api$assign_next()
#' dput(result)
#'
#'
#' ####################  list  ####################
#'
#' library(MosquitoAlert)
#' var_annotator_ids <- c(123) # array[integer] |  (Optional)
#' var_assignee_ids <- c(123) # array[integer] |  (Optional)
#' var_created_at_after <- "created_at_after_example" # character | Created at (Optional)
#' var_created_at_before <- "created_at_before_example" # character | Created at (Optional)
#' var_fully_predicted <- "fully_predicted_example" # character | Filters identification task based on whether all associated photos have predictions. Set to True to include identification tasks where every photo has a prediction; set to False to include identification tasks where at least one photo is missing a prediction. (Optional)
#' var_is_flagged <- "is_flagged_example" # character |  (Optional)
#' var_is_safe <- "is_safe_example" # character |  (Optional)
#' var_num_annotations_max <- 56 # integer |  (Optional)
#' var_num_annotations_min <- 56 # integer |  (Optional)
#' var_observation_country_ids <- c(123) # array[integer] |  (Optional)
#' var_order_by <- c("-created_at") # array[character] | Ordenado   (Optional)
#' var_page <- 56 # integer | A page number within the paginated result set. (Optional)
#' var_page_size <- 56 # integer | Number of results to return per page. (Optional)
#' var_result_agreement_max <- 3.4 # numeric |  (Optional)
#' var_result_agreement_min <- 3.4 # numeric |  (Optional)
#' var_result_confidence_max <- 3.4 # numeric |  (Optional)
#' var_result_confidence_min <- 3.4 # numeric |  (Optional)
#' var_result_source <- c("ai") # array[character] |  (Optional)
#' var_result_taxon_ids <- c(123) # array[integer] |  (Optional)
#' var_result_uncertainty_max <- 3.4 # numeric |  (Optional)
#' var_result_uncertainty_min <- 3.4 # numeric |  (Optional)
#' var_review_type <- "review_type_example" # character |  (Optional)
#' var_status <- c("archived") # array[character] |  (Optional)
#' var_updated_at_after <- "updated_at_after_example" # character | Update at (Optional)
#' var_updated_at_before <- "updated_at_before_example" # character | Update at (Optional)
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$list(annotator_ids = var_annotator_ids, assignee_ids = var_assignee_ids, created_at_after = var_created_at_after, created_at_before = var_created_at_before, fully_predicted = var_fully_predicted, is_flagged = var_is_flagged, is_safe = var_is_safe, num_annotations_max = var_num_annotations_max, num_annotations_min = var_num_annotations_min, observation_country_ids = var_observation_country_ids, order_by = var_order_by, page = var_page, page_size = var_page_size, result_agreement_max = var_result_agreement_max, result_agreement_min = var_result_agreement_min, result_confidence_max = var_result_confidence_max, result_confidence_min = var_result_confidence_min, result_source = var_result_source, result_taxon_ids = var_result_taxon_ids, result_uncertainty_max = var_result_uncertainty_max, result_uncertainty_min = var_result_uncertainty_min, review_type = var_review_type, status = var_status, updated_at_after = var_updated_at_after, updated_at_before = var_updated_at_beforedata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$list(annotator_ids = var_annotator_ids, assignee_ids = var_assignee_ids, created_at_after = var_created_at_after, created_at_before = var_created_at_before, fully_predicted = var_fully_predicted, is_flagged = var_is_flagged, is_safe = var_is_safe, num_annotations_max = var_num_annotations_max, num_annotations_min = var_num_annotations_min, observation_country_ids = var_observation_country_ids, order_by = var_order_by, page = var_page, page_size = var_page_size, result_agreement_max = var_result_agreement_max, result_agreement_min = var_result_agreement_min, result_confidence_max = var_result_confidence_max, result_confidence_min = var_result_confidence_min, result_source = var_result_source, result_taxon_ids = var_result_taxon_ids, result_uncertainty_max = var_result_uncertainty_max, result_uncertainty_min = var_result_uncertainty_min, review_type = var_review_type, status = var_status, updated_at_after = var_updated_at_after, updated_at_before = var_updated_at_before)
#' dput(result)
#'
#'
#' ####################  list_mine  ####################
#'
#' library(MosquitoAlert)
#' var_annotator_ids <- c(123) # array[integer] |  (Optional)
#' var_assignee_ids <- c(123) # array[integer] |  (Optional)
#' var_created_at_after <- "created_at_after_example" # character | Created at (Optional)
#' var_created_at_before <- "created_at_before_example" # character | Created at (Optional)
#' var_fully_predicted <- "fully_predicted_example" # character | Filters identification task based on whether all associated photos have predictions. Set to True to include identification tasks where every photo has a prediction; set to False to include identification tasks where at least one photo is missing a prediction. (Optional)
#' var_is_flagged <- "is_flagged_example" # character |  (Optional)
#' var_is_safe <- "is_safe_example" # character |  (Optional)
#' var_num_annotations_max <- 56 # integer |  (Optional)
#' var_num_annotations_min <- 56 # integer |  (Optional)
#' var_observation_country_ids <- c(123) # array[integer] |  (Optional)
#' var_order_by <- c("-created_at") # array[character] | Ordenado   (Optional)
#' var_page <- 56 # integer | A page number within the paginated result set. (Optional)
#' var_page_size <- 56 # integer | Number of results to return per page. (Optional)
#' var_result_agreement_max <- 3.4 # numeric |  (Optional)
#' var_result_agreement_min <- 3.4 # numeric |  (Optional)
#' var_result_confidence_max <- 3.4 # numeric |  (Optional)
#' var_result_confidence_min <- 3.4 # numeric |  (Optional)
#' var_result_source <- c("ai") # array[character] |  (Optional)
#' var_result_taxon_ids <- c(123) # array[integer] |  (Optional)
#' var_result_uncertainty_max <- 3.4 # numeric |  (Optional)
#' var_result_uncertainty_min <- 3.4 # numeric |  (Optional)
#' var_review_type <- "review_type_example" # character |  (Optional)
#' var_status <- c("archived") # array[character] |  (Optional)
#' var_updated_at_after <- "updated_at_after_example" # character | Update at (Optional)
#' var_updated_at_before <- "updated_at_before_example" # character | Update at (Optional)
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$list_mine(annotator_ids = var_annotator_ids, assignee_ids = var_assignee_ids, created_at_after = var_created_at_after, created_at_before = var_created_at_before, fully_predicted = var_fully_predicted, is_flagged = var_is_flagged, is_safe = var_is_safe, num_annotations_max = var_num_annotations_max, num_annotations_min = var_num_annotations_min, observation_country_ids = var_observation_country_ids, order_by = var_order_by, page = var_page, page_size = var_page_size, result_agreement_max = var_result_agreement_max, result_agreement_min = var_result_agreement_min, result_confidence_max = var_result_confidence_max, result_confidence_min = var_result_confidence_min, result_source = var_result_source, result_taxon_ids = var_result_taxon_ids, result_uncertainty_max = var_result_uncertainty_max, result_uncertainty_min = var_result_uncertainty_min, review_type = var_review_type, status = var_status, updated_at_after = var_updated_at_after, updated_at_before = var_updated_at_beforedata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$list_mine(annotator_ids = var_annotator_ids, assignee_ids = var_assignee_ids, created_at_after = var_created_at_after, created_at_before = var_created_at_before, fully_predicted = var_fully_predicted, is_flagged = var_is_flagged, is_safe = var_is_safe, num_annotations_max = var_num_annotations_max, num_annotations_min = var_num_annotations_min, observation_country_ids = var_observation_country_ids, order_by = var_order_by, page = var_page, page_size = var_page_size, result_agreement_max = var_result_agreement_max, result_agreement_min = var_result_agreement_min, result_confidence_max = var_result_confidence_max, result_confidence_min = var_result_confidence_min, result_source = var_result_source, result_taxon_ids = var_result_taxon_ids, result_uncertainty_max = var_result_uncertainty_max, result_uncertainty_min = var_result_uncertainty_min, review_type = var_review_type, status = var_status, updated_at_after = var_updated_at_after, updated_at_before = var_updated_at_before)
#' dput(result)
#'
#'
#' ####################  predictions_create  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#' var_create_photo_prediction_request <- CreatePhotoPredictionRequest$new("photo_uuid_example", BoundingBoxRequest$new(123, 123, 123, 123), 123, "ae_albopictus", 123, PredictionScoreRequest$new(123, 123, 123, 123, 123, 123, 123, 123, 123), "v2023.1", "is_decisive_example") # CreatePhotoPredictionRequest | 
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$predictions_create(var_observation_uuid, var_create_photo_prediction_requestdata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$predictions_create(var_observation_uuid, var_create_photo_prediction_request)
#' dput(result)
#'
#'
#' ####################  predictions_destroy  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#' var_photo_uuid <- "photo_uuid_example" # character | 
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$identification_tasks_api$predictions_destroy(var_observation_uuid, var_photo_uuid)
#'
#'
#' ####################  predictions_list  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#' var_page <- 56 # integer | A page number within the paginated result set. (Optional)
#' var_page_size <- 56 # integer | Number of results to return per page. (Optional)
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$predictions_list(var_observation_uuid, page = var_page, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$predictions_list(var_observation_uuid, page = var_page, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  predictions_partial_update  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#' var_photo_uuid <- "photo_uuid_example" # character | 
#' var_patched_photo_prediction_request <- PatchedPhotoPredictionRequest$new(BoundingBoxRequest$new(123, 123, 123, 123), 123, "ae_albopictus", 123, "is_decisive_example", PredictionScoreRequest$new(123, 123, 123, 123, 123, 123, 123, 123, 123), "v2023.1") # PatchedPhotoPredictionRequest |  (Optional)
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$predictions_partial_update(var_observation_uuid, var_photo_uuid, patched_photo_prediction_request = var_patched_photo_prediction_requestdata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$predictions_partial_update(var_observation_uuid, var_photo_uuid, patched_photo_prediction_request = var_patched_photo_prediction_request)
#' dput(result)
#'
#'
#' ####################  predictions_retrieve  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#' var_photo_uuid <- "photo_uuid_example" # character | 
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$predictions_retrieve(var_observation_uuid, var_photo_uuiddata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$predictions_retrieve(var_observation_uuid, var_photo_uuid)
#' dput(result)
#'
#'
#' ####################  predictions_update  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | UUID of the Observation
#' var_photo_uuid <- "photo_uuid_example" # character | 
#' var_photo_prediction_request <- PhotoPredictionRequest$new(BoundingBoxRequest$new(123, 123, 123, 123), 123, "ae_albopictus", 123, PredictionScoreRequest$new(123, 123, 123, 123, 123, 123, 123, 123, 123), "v2023.1", "is_decisive_example") # PhotoPredictionRequest | 
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$predictions_update(var_observation_uuid, var_photo_uuid, var_photo_prediction_requestdata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$predictions_update(var_observation_uuid, var_photo_uuid, var_photo_prediction_request)
#' dput(result)
#'
#'
#' ####################  retrieve  ####################
#'
#' library(MosquitoAlert)
#' var_observation_uuid <- "observation_uuid_example" # character | 
#'
#' api_instance <- mosquitoalert_api$new()
#'
#' # Configure API key authorization: tokenAuth
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: cookieAuth
#' api_instance$api_client$api_keys["sessionid"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: jwtAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$retrieve(var_observation_uuiddata_file = "result.txt")
#' result <- api_instance$identification_tasks_api$retrieve(var_observation_uuid)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
IdentificationTasksApi <- R6::R6Class(
  "IdentificationTasksApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new IdentificationTasksApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param annotation_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Annotation
    annotations_create = function(observation_uuid, annotation_request, data_file = NULL, ...) {
      local_var_response <- self$annotations_create_with_http_info(observation_uuid, annotation_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param annotation_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Annotation) with additional information such as HTTP status code, headers
    annotations_create_with_http_info = function(observation_uuid, annotation_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }

      if (missing(`annotation_request`)) {
        stop("Missing required parameter `annotation_request`.")
      }



      if (!is.null(`annotation_request`)) {
        local_var_body <- `annotation_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/identification-tasks/{observation_uuid}/annotations/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "application/x-www-form-urlencoded", "multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Annotation"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param classification_confidence_label (optional) No description
    #' @param classification_confidence_max (optional) No description
    #' @param classification_confidence_min (optional) No description
    #' @param classification_taxon_ids (optional) No description
    #' @param created_at_after (optional) Created at
    #' @param created_at_before (optional) Created at
    #' @param is_decisive (optional) No description
    #' @param is_favourite (optional) No description
    #' @param is_flagged (optional) No description
    #' @param order_by (optional) Ordenado  
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param type (optional) No description
    #' @param updated_at_after (optional) Updated at
    #' @param updated_at_before (optional) Updated at
    #' @param user_ids (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PaginatedAnnotationList
    annotations_list = function(observation_uuid, classification_confidence_label = NULL, classification_confidence_max = NULL, classification_confidence_min = NULL, classification_taxon_ids = NULL, created_at_after = NULL, created_at_before = NULL, is_decisive = NULL, is_favourite = NULL, is_flagged = NULL, order_by = NULL, page = NULL, page_size = NULL, type = NULL, updated_at_after = NULL, updated_at_before = NULL, user_ids = NULL, data_file = NULL, ...) {
      local_var_response <- self$annotations_list_with_http_info(observation_uuid, classification_confidence_label, classification_confidence_max, classification_confidence_min, classification_taxon_ids, created_at_after, created_at_before, is_decisive, is_favourite, is_flagged, order_by, page, page_size, type, updated_at_after, updated_at_before, user_ids, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param classification_confidence_label (optional) No description
    #' @param classification_confidence_max (optional) No description
    #' @param classification_confidence_min (optional) No description
    #' @param classification_taxon_ids (optional) No description
    #' @param created_at_after (optional) Created at
    #' @param created_at_before (optional) Created at
    #' @param is_decisive (optional) No description
    #' @param is_favourite (optional) No description
    #' @param is_flagged (optional) No description
    #' @param order_by (optional) Ordenado  
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param type (optional) No description
    #' @param updated_at_after (optional) Updated at
    #' @param updated_at_before (optional) Updated at
    #' @param user_ids (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PaginatedAnnotationList) with additional information such as HTTP status code, headers
    annotations_list_with_http_info = function(observation_uuid, classification_confidence_label = NULL, classification_confidence_max = NULL, classification_confidence_min = NULL, classification_taxon_ids = NULL, created_at_after = NULL, created_at_before = NULL, is_decisive = NULL, is_favourite = NULL, is_flagged = NULL, order_by = NULL, page = NULL, page_size = NULL, type = NULL, updated_at_after = NULL, updated_at_before = NULL, user_ids = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }



      if (`classification_confidence_max` > 1.0) {
        stop("Invalid value for `classification_confidence_max` when calling IdentificationTasksApi$annotations_list, must be smaller than or equal to 1.0.")
      }
      if (`classification_confidence_max` < 0.0) {
        stop("Invalid value for `classification_confidence_max` when calling IdentificationTasksApi$annotations_list, must be bigger than or equal to 0.0.")
      }

      if (`classification_confidence_min` > 1.0) {
        stop("Invalid value for `classification_confidence_min` when calling IdentificationTasksApi$annotations_list, must be smaller than or equal to 1.0.")
      }
      if (`classification_confidence_min` < 0.0) {
        stop("Invalid value for `classification_confidence_min` when calling IdentificationTasksApi$annotations_list, must be bigger than or equal to 0.0.")
      }














      if (!is.null(`classification_confidence_label`) && !(`classification_confidence_label` %in% c("definitely", "probably"))) {
        stop("Invalid value for classification_confidence_label when calling IdentificationTasksApi$annotations_list. Must be [definitely, probably].")
      }
      query_params[["classification_confidence_label"]] <- `classification_confidence_label`

      query_params[["classification_confidence_max"]] <- `classification_confidence_max`

      query_params[["classification_confidence_min"]] <- `classification_confidence_min`

      # explore
      for (query_item in `classification_taxon_ids`) {
        query_params[["classification_taxon_ids"]] <- c(query_params[["classification_taxon_ids"]], list(`classification_taxon_ids` = query_item))
      }

      query_params[["created_at_after"]] <- `created_at_after`

      query_params[["created_at_before"]] <- `created_at_before`

      query_params[["is_decisive"]] <- `is_decisive`

      query_params[["is_favourite"]] <- `is_favourite`

      query_params[["is_flagged"]] <- `is_flagged`

      # no explore
      # validate enum values
      for (query_item in `order_by`) {
        if (!is.null(query_item) && !(query_item %in% c("-created_at", "-updated_at", "created_at", "updated_at"))) {
          stop("Invalid value for order_by when calling IdentificationTasksApi$annotations_list. Must be [-created_at, -updated_at, created_at, updated_at].")
        }
      }
      query_params[["order_by"]] <- I(paste(lapply(`order_by`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["page"]] <- `page`

      query_params[["page_size"]] <- `page_size`

      if (!is.null(`type`) && !(`type` %in% c("long", "short"))) {
        stop("Invalid value for type when calling IdentificationTasksApi$annotations_list. Must be [long, short].")
      }
      query_params[["type"]] <- `type`

      query_params[["updated_at_after"]] <- `updated_at_after`

      query_params[["updated_at_before"]] <- `updated_at_before`

      # explore
      for (query_item in `user_ids`) {
        query_params[["user_ids"]] <- c(query_params[["user_ids"]], list(`user_ids` = query_item))
      }

      local_var_url_path <- "/identification-tasks/{observation_uuid}/annotations/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaginatedAnnotationList"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param classification_confidence_label (optional) No description
    #' @param classification_confidence_max (optional) No description
    #' @param classification_confidence_min (optional) No description
    #' @param classification_taxon_ids (optional) No description
    #' @param created_at_after (optional) Created at
    #' @param created_at_before (optional) Created at
    #' @param is_decisive (optional) No description
    #' @param is_favourite (optional) No description
    #' @param is_flagged (optional) No description
    #' @param order_by (optional) Ordenado  
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param type (optional) No description
    #' @param updated_at_after (optional) Updated at
    #' @param updated_at_before (optional) Updated at
    #' @param user_ids (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PaginatedAnnotationList
    annotations_list_mine = function(classification_confidence_label = NULL, classification_confidence_max = NULL, classification_confidence_min = NULL, classification_taxon_ids = NULL, created_at_after = NULL, created_at_before = NULL, is_decisive = NULL, is_favourite = NULL, is_flagged = NULL, order_by = NULL, page = NULL, page_size = NULL, type = NULL, updated_at_after = NULL, updated_at_before = NULL, user_ids = NULL, data_file = NULL, ...) {
      local_var_response <- self$annotations_list_mine_with_http_info(classification_confidence_label, classification_confidence_max, classification_confidence_min, classification_taxon_ids, created_at_after, created_at_before, is_decisive, is_favourite, is_flagged, order_by, page, page_size, type, updated_at_after, updated_at_before, user_ids, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param classification_confidence_label (optional) No description
    #' @param classification_confidence_max (optional) No description
    #' @param classification_confidence_min (optional) No description
    #' @param classification_taxon_ids (optional) No description
    #' @param created_at_after (optional) Created at
    #' @param created_at_before (optional) Created at
    #' @param is_decisive (optional) No description
    #' @param is_favourite (optional) No description
    #' @param is_flagged (optional) No description
    #' @param order_by (optional) Ordenado  
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param type (optional) No description
    #' @param updated_at_after (optional) Updated at
    #' @param updated_at_before (optional) Updated at
    #' @param user_ids (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PaginatedAnnotationList) with additional information such as HTTP status code, headers
    annotations_list_mine_with_http_info = function(classification_confidence_label = NULL, classification_confidence_max = NULL, classification_confidence_min = NULL, classification_taxon_ids = NULL, created_at_after = NULL, created_at_before = NULL, is_decisive = NULL, is_favourite = NULL, is_flagged = NULL, order_by = NULL, page = NULL, page_size = NULL, type = NULL, updated_at_after = NULL, updated_at_before = NULL, user_ids = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (`classification_confidence_max` > 1.0) {
        stop("Invalid value for `classification_confidence_max` when calling IdentificationTasksApi$annotations_list_mine, must be smaller than or equal to 1.0.")
      }
      if (`classification_confidence_max` < 0.0) {
        stop("Invalid value for `classification_confidence_max` when calling IdentificationTasksApi$annotations_list_mine, must be bigger than or equal to 0.0.")
      }

      if (`classification_confidence_min` > 1.0) {
        stop("Invalid value for `classification_confidence_min` when calling IdentificationTasksApi$annotations_list_mine, must be smaller than or equal to 1.0.")
      }
      if (`classification_confidence_min` < 0.0) {
        stop("Invalid value for `classification_confidence_min` when calling IdentificationTasksApi$annotations_list_mine, must be bigger than or equal to 0.0.")
      }














      if (!is.null(`classification_confidence_label`) && !(`classification_confidence_label` %in% c("definitely", "probably"))) {
        stop("Invalid value for classification_confidence_label when calling IdentificationTasksApi$annotations_list_mine. Must be [definitely, probably].")
      }
      query_params[["classification_confidence_label"]] <- `classification_confidence_label`

      query_params[["classification_confidence_max"]] <- `classification_confidence_max`

      query_params[["classification_confidence_min"]] <- `classification_confidence_min`

      # explore
      for (query_item in `classification_taxon_ids`) {
        query_params[["classification_taxon_ids"]] <- c(query_params[["classification_taxon_ids"]], list(`classification_taxon_ids` = query_item))
      }

      query_params[["created_at_after"]] <- `created_at_after`

      query_params[["created_at_before"]] <- `created_at_before`

      query_params[["is_decisive"]] <- `is_decisive`

      query_params[["is_favourite"]] <- `is_favourite`

      query_params[["is_flagged"]] <- `is_flagged`

      # no explore
      # validate enum values
      for (query_item in `order_by`) {
        if (!is.null(query_item) && !(query_item %in% c("-created_at", "-updated_at", "created_at", "updated_at"))) {
          stop("Invalid value for order_by when calling IdentificationTasksApi$annotations_list_mine. Must be [-created_at, -updated_at, created_at, updated_at].")
        }
      }
      query_params[["order_by"]] <- I(paste(lapply(`order_by`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["page"]] <- `page`

      query_params[["page_size"]] <- `page_size`

      if (!is.null(`type`) && !(`type` %in% c("long", "short"))) {
        stop("Invalid value for type when calling IdentificationTasksApi$annotations_list_mine. Must be [long, short].")
      }
      query_params[["type"]] <- `type`

      query_params[["updated_at_after"]] <- `updated_at_after`

      query_params[["updated_at_before"]] <- `updated_at_before`

      # explore
      for (query_item in `user_ids`) {
        query_params[["user_ids"]] <- c(query_params[["user_ids"]], list(`user_ids` = query_item))
      }

      local_var_url_path <- "/me/identification-tasks/annotations/"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaginatedAnnotationList"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param id A unique integer value identifying this expert report annotation.
    #' @param observation_uuid UUID of the Observation
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Annotation
    annotations_retrieve = function(id, observation_uuid, data_file = NULL, ...) {
      local_var_response <- self$annotations_retrieve_with_http_info(id, observation_uuid, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param id A unique integer value identifying this expert report annotation.
    #' @param observation_uuid UUID of the Observation
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Annotation) with additional information such as HTTP status code, headers
    annotations_retrieve_with_http_info = function(id, observation_uuid, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }



      local_var_url_path <- "/identification-tasks/{observation_uuid}/annotations/{id}/"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Annotation"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Assignment
    assign_next = function(data_file = NULL, ...) {
      local_var_response <- self$assign_next_with_http_info(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Assignment) with additional information such as HTTP status code, headers
    assign_next_with_http_info = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/identification-tasks/assignments/next/"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Assignment"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param annotator_ids (optional) No description
    #' @param assignee_ids (optional) No description
    #' @param created_at_after (optional) Created at
    #' @param created_at_before (optional) Created at
    #' @param fully_predicted (optional) Filters identification task based on whether all associated photos have predictions. Set to True to include identification tasks where every photo has a prediction; set to False to include identification tasks where at least one photo is missing a prediction.
    #' @param is_flagged (optional) No description
    #' @param is_safe (optional) No description
    #' @param num_annotations_max (optional) No description
    #' @param num_annotations_min (optional) No description
    #' @param observation_country_ids (optional) No description
    #' @param order_by (optional) Ordenado  
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param result_agreement_max (optional) No description
    #' @param result_agreement_min (optional) No description
    #' @param result_confidence_max (optional) No description
    #' @param result_confidence_min (optional) No description
    #' @param result_source (optional) No description
    #' @param result_taxon_ids (optional) No description
    #' @param result_uncertainty_max (optional) No description
    #' @param result_uncertainty_min (optional) No description
    #' @param review_type (optional) No description
    #' @param status (optional) No description
    #' @param updated_at_after (optional) Update at
    #' @param updated_at_before (optional) Update at
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PaginatedIdentificationTaskList
    list = function(annotator_ids = NULL, assignee_ids = NULL, created_at_after = NULL, created_at_before = NULL, fully_predicted = NULL, is_flagged = NULL, is_safe = NULL, num_annotations_max = NULL, num_annotations_min = NULL, observation_country_ids = NULL, order_by = NULL, page = NULL, page_size = NULL, result_agreement_max = NULL, result_agreement_min = NULL, result_confidence_max = NULL, result_confidence_min = NULL, result_source = NULL, result_taxon_ids = NULL, result_uncertainty_max = NULL, result_uncertainty_min = NULL, review_type = NULL, status = NULL, updated_at_after = NULL, updated_at_before = NULL, data_file = NULL, ...) {
      local_var_response <- self$list_with_http_info(annotator_ids, assignee_ids, created_at_after, created_at_before, fully_predicted, is_flagged, is_safe, num_annotations_max, num_annotations_min, observation_country_ids, order_by, page, page_size, result_agreement_max, result_agreement_min, result_confidence_max, result_confidence_min, result_source, result_taxon_ids, result_uncertainty_max, result_uncertainty_min, review_type, status, updated_at_after, updated_at_before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param annotator_ids (optional) No description
    #' @param assignee_ids (optional) No description
    #' @param created_at_after (optional) Created at
    #' @param created_at_before (optional) Created at
    #' @param fully_predicted (optional) Filters identification task based on whether all associated photos have predictions. Set to True to include identification tasks where every photo has a prediction; set to False to include identification tasks where at least one photo is missing a prediction.
    #' @param is_flagged (optional) No description
    #' @param is_safe (optional) No description
    #' @param num_annotations_max (optional) No description
    #' @param num_annotations_min (optional) No description
    #' @param observation_country_ids (optional) No description
    #' @param order_by (optional) Ordenado  
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param result_agreement_max (optional) No description
    #' @param result_agreement_min (optional) No description
    #' @param result_confidence_max (optional) No description
    #' @param result_confidence_min (optional) No description
    #' @param result_source (optional) No description
    #' @param result_taxon_ids (optional) No description
    #' @param result_uncertainty_max (optional) No description
    #' @param result_uncertainty_min (optional) No description
    #' @param review_type (optional) No description
    #' @param status (optional) No description
    #' @param updated_at_after (optional) Update at
    #' @param updated_at_before (optional) Update at
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PaginatedIdentificationTaskList) with additional information such as HTTP status code, headers
    list_with_http_info = function(annotator_ids = NULL, assignee_ids = NULL, created_at_after = NULL, created_at_before = NULL, fully_predicted = NULL, is_flagged = NULL, is_safe = NULL, num_annotations_max = NULL, num_annotations_min = NULL, observation_country_ids = NULL, order_by = NULL, page = NULL, page_size = NULL, result_agreement_max = NULL, result_agreement_min = NULL, result_confidence_max = NULL, result_confidence_min = NULL, result_source = NULL, result_taxon_ids = NULL, result_uncertainty_max = NULL, result_uncertainty_min = NULL, review_type = NULL, status = NULL, updated_at_after = NULL, updated_at_before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE
















      if (!str_detect(`result_confidence_max`, "^-?\\d{0,1}(?:\\.\\d{0,6})?$")) {
        stop("Invalid value for `result_confidence_max` when calling IdentificationTasksApi$list, must conform to the pattern ^-?\\d{0,1}(?:\\.\\d{0,6})?$.")
      }

      if (!str_detect(`result_confidence_min`, "^-?\\d{0,1}(?:\\.\\d{0,6})?$")) {
        stop("Invalid value for `result_confidence_min` when calling IdentificationTasksApi$list, must conform to the pattern ^-?\\d{0,1}(?:\\.\\d{0,6})?$.")
      }









      # explore
      for (query_item in `annotator_ids`) {
        query_params[["annotator_ids"]] <- c(query_params[["annotator_ids"]], list(`annotator_ids` = query_item))
      }

      # explore
      for (query_item in `assignee_ids`) {
        query_params[["assignee_ids"]] <- c(query_params[["assignee_ids"]], list(`assignee_ids` = query_item))
      }

      query_params[["created_at_after"]] <- `created_at_after`

      query_params[["created_at_before"]] <- `created_at_before`

      query_params[["fully_predicted"]] <- `fully_predicted`

      query_params[["is_flagged"]] <- `is_flagged`

      query_params[["is_safe"]] <- `is_safe`

      query_params[["num_annotations_max"]] <- `num_annotations_max`

      query_params[["num_annotations_min"]] <- `num_annotations_min`

      # explore
      for (query_item in `observation_country_ids`) {
        query_params[["observation_country_ids"]] <- c(query_params[["observation_country_ids"]], list(`observation_country_ids` = query_item))
      }

      # no explore
      # validate enum values
      for (query_item in `order_by`) {
        if (!is.null(query_item) && !(query_item %in% c("-created_at", "-updated_at", "created_at", "updated_at"))) {
          stop("Invalid value for order_by when calling IdentificationTasksApi$list. Must be [-created_at, -updated_at, created_at, updated_at].")
        }
      }
      query_params[["order_by"]] <- I(paste(lapply(`order_by`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["page"]] <- `page`

      query_params[["page_size"]] <- `page_size`

      query_params[["result_agreement_max"]] <- `result_agreement_max`

      query_params[["result_agreement_min"]] <- `result_agreement_min`

      query_params[["result_confidence_max"]] <- `result_confidence_max`

      query_params[["result_confidence_min"]] <- `result_confidence_min`

      # explore
      for (query_item in `result_source`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("ai", "expert"))) {
          stop("Invalid value for result_source when calling IdentificationTasksApi$list. Must be [ai, expert].")
        }
        query_params[["result_source"]] <- c(query_params[["result_source"]], list(`result_source` = query_item))
      }

      # explore
      for (query_item in `result_taxon_ids`) {
        query_params[["result_taxon_ids"]] <- c(query_params[["result_taxon_ids"]], list(`result_taxon_ids` = query_item))
      }

      query_params[["result_uncertainty_max"]] <- `result_uncertainty_max`

      query_params[["result_uncertainty_min"]] <- `result_uncertainty_min`

      if (!is.null(`review_type`) && !(`review_type` %in% c("agree", "overwrite"))) {
        stop("Invalid value for review_type when calling IdentificationTasksApi$list. Must be [agree, overwrite].")
      }
      query_params[["review_type"]] <- `review_type`

      # explore
      for (query_item in `status`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("archived", "conflict", "done", "open", "review"))) {
          stop("Invalid value for status when calling IdentificationTasksApi$list. Must be [archived, conflict, done, open, review].")
        }
        query_params[["status"]] <- c(query_params[["status"]], list(`status` = query_item))
      }

      query_params[["updated_at_after"]] <- `updated_at_after`

      query_params[["updated_at_before"]] <- `updated_at_before`

      local_var_url_path <- "/identification-tasks/"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaginatedIdentificationTaskList"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param annotator_ids (optional) No description
    #' @param assignee_ids (optional) No description
    #' @param created_at_after (optional) Created at
    #' @param created_at_before (optional) Created at
    #' @param fully_predicted (optional) Filters identification task based on whether all associated photos have predictions. Set to True to include identification tasks where every photo has a prediction; set to False to include identification tasks where at least one photo is missing a prediction.
    #' @param is_flagged (optional) No description
    #' @param is_safe (optional) No description
    #' @param num_annotations_max (optional) No description
    #' @param num_annotations_min (optional) No description
    #' @param observation_country_ids (optional) No description
    #' @param order_by (optional) Ordenado  
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param result_agreement_max (optional) No description
    #' @param result_agreement_min (optional) No description
    #' @param result_confidence_max (optional) No description
    #' @param result_confidence_min (optional) No description
    #' @param result_source (optional) No description
    #' @param result_taxon_ids (optional) No description
    #' @param result_uncertainty_max (optional) No description
    #' @param result_uncertainty_min (optional) No description
    #' @param review_type (optional) No description
    #' @param status (optional) No description
    #' @param updated_at_after (optional) Update at
    #' @param updated_at_before (optional) Update at
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PaginatedIdentificationTaskList
    list_mine = function(annotator_ids = NULL, assignee_ids = NULL, created_at_after = NULL, created_at_before = NULL, fully_predicted = NULL, is_flagged = NULL, is_safe = NULL, num_annotations_max = NULL, num_annotations_min = NULL, observation_country_ids = NULL, order_by = NULL, page = NULL, page_size = NULL, result_agreement_max = NULL, result_agreement_min = NULL, result_confidence_max = NULL, result_confidence_min = NULL, result_source = NULL, result_taxon_ids = NULL, result_uncertainty_max = NULL, result_uncertainty_min = NULL, review_type = NULL, status = NULL, updated_at_after = NULL, updated_at_before = NULL, data_file = NULL, ...) {
      local_var_response <- self$list_mine_with_http_info(annotator_ids, assignee_ids, created_at_after, created_at_before, fully_predicted, is_flagged, is_safe, num_annotations_max, num_annotations_min, observation_country_ids, order_by, page, page_size, result_agreement_max, result_agreement_min, result_confidence_max, result_confidence_min, result_source, result_taxon_ids, result_uncertainty_max, result_uncertainty_min, review_type, status, updated_at_after, updated_at_before, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param annotator_ids (optional) No description
    #' @param assignee_ids (optional) No description
    #' @param created_at_after (optional) Created at
    #' @param created_at_before (optional) Created at
    #' @param fully_predicted (optional) Filters identification task based on whether all associated photos have predictions. Set to True to include identification tasks where every photo has a prediction; set to False to include identification tasks where at least one photo is missing a prediction.
    #' @param is_flagged (optional) No description
    #' @param is_safe (optional) No description
    #' @param num_annotations_max (optional) No description
    #' @param num_annotations_min (optional) No description
    #' @param observation_country_ids (optional) No description
    #' @param order_by (optional) Ordenado  
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param result_agreement_max (optional) No description
    #' @param result_agreement_min (optional) No description
    #' @param result_confidence_max (optional) No description
    #' @param result_confidence_min (optional) No description
    #' @param result_source (optional) No description
    #' @param result_taxon_ids (optional) No description
    #' @param result_uncertainty_max (optional) No description
    #' @param result_uncertainty_min (optional) No description
    #' @param review_type (optional) No description
    #' @param status (optional) No description
    #' @param updated_at_after (optional) Update at
    #' @param updated_at_before (optional) Update at
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PaginatedIdentificationTaskList) with additional information such as HTTP status code, headers
    list_mine_with_http_info = function(annotator_ids = NULL, assignee_ids = NULL, created_at_after = NULL, created_at_before = NULL, fully_predicted = NULL, is_flagged = NULL, is_safe = NULL, num_annotations_max = NULL, num_annotations_min = NULL, observation_country_ids = NULL, order_by = NULL, page = NULL, page_size = NULL, result_agreement_max = NULL, result_agreement_min = NULL, result_confidence_max = NULL, result_confidence_min = NULL, result_source = NULL, result_taxon_ids = NULL, result_uncertainty_max = NULL, result_uncertainty_min = NULL, review_type = NULL, status = NULL, updated_at_after = NULL, updated_at_before = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE
















      if (!str_detect(`result_confidence_max`, "^-?\\d{0,1}(?:\\.\\d{0,6})?$")) {
        stop("Invalid value for `result_confidence_max` when calling IdentificationTasksApi$list_mine, must conform to the pattern ^-?\\d{0,1}(?:\\.\\d{0,6})?$.")
      }

      if (!str_detect(`result_confidence_min`, "^-?\\d{0,1}(?:\\.\\d{0,6})?$")) {
        stop("Invalid value for `result_confidence_min` when calling IdentificationTasksApi$list_mine, must conform to the pattern ^-?\\d{0,1}(?:\\.\\d{0,6})?$.")
      }









      # explore
      for (query_item in `annotator_ids`) {
        query_params[["annotator_ids"]] <- c(query_params[["annotator_ids"]], list(`annotator_ids` = query_item))
      }

      # explore
      for (query_item in `assignee_ids`) {
        query_params[["assignee_ids"]] <- c(query_params[["assignee_ids"]], list(`assignee_ids` = query_item))
      }

      query_params[["created_at_after"]] <- `created_at_after`

      query_params[["created_at_before"]] <- `created_at_before`

      query_params[["fully_predicted"]] <- `fully_predicted`

      query_params[["is_flagged"]] <- `is_flagged`

      query_params[["is_safe"]] <- `is_safe`

      query_params[["num_annotations_max"]] <- `num_annotations_max`

      query_params[["num_annotations_min"]] <- `num_annotations_min`

      # explore
      for (query_item in `observation_country_ids`) {
        query_params[["observation_country_ids"]] <- c(query_params[["observation_country_ids"]], list(`observation_country_ids` = query_item))
      }

      # no explore
      # validate enum values
      for (query_item in `order_by`) {
        if (!is.null(query_item) && !(query_item %in% c("-created_at", "-updated_at", "created_at", "updated_at"))) {
          stop("Invalid value for order_by when calling IdentificationTasksApi$list_mine. Must be [-created_at, -updated_at, created_at, updated_at].")
        }
      }
      query_params[["order_by"]] <- I(paste(lapply(`order_by`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["page"]] <- `page`

      query_params[["page_size"]] <- `page_size`

      query_params[["result_agreement_max"]] <- `result_agreement_max`

      query_params[["result_agreement_min"]] <- `result_agreement_min`

      query_params[["result_confidence_max"]] <- `result_confidence_max`

      query_params[["result_confidence_min"]] <- `result_confidence_min`

      # explore
      for (query_item in `result_source`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("ai", "expert"))) {
          stop("Invalid value for result_source when calling IdentificationTasksApi$list_mine. Must be [ai, expert].")
        }
        query_params[["result_source"]] <- c(query_params[["result_source"]], list(`result_source` = query_item))
      }

      # explore
      for (query_item in `result_taxon_ids`) {
        query_params[["result_taxon_ids"]] <- c(query_params[["result_taxon_ids"]], list(`result_taxon_ids` = query_item))
      }

      query_params[["result_uncertainty_max"]] <- `result_uncertainty_max`

      query_params[["result_uncertainty_min"]] <- `result_uncertainty_min`

      if (!is.null(`review_type`) && !(`review_type` %in% c("agree", "overwrite"))) {
        stop("Invalid value for review_type when calling IdentificationTasksApi$list_mine. Must be [agree, overwrite].")
      }
      query_params[["review_type"]] <- `review_type`

      # explore
      for (query_item in `status`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("archived", "conflict", "done", "open", "review"))) {
          stop("Invalid value for status when calling IdentificationTasksApi$list_mine. Must be [archived, conflict, done, open, review].")
        }
        query_params[["status"]] <- c(query_params[["status"]], list(`status` = query_item))
      }

      query_params[["updated_at_after"]] <- `updated_at_after`

      query_params[["updated_at_before"]] <- `updated_at_before`

      local_var_url_path <- "/me/identification-tasks/"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaginatedIdentificationTaskList"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param create_photo_prediction_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CreatePhotoPrediction
    predictions_create = function(observation_uuid, create_photo_prediction_request, data_file = NULL, ...) {
      local_var_response <- self$predictions_create_with_http_info(observation_uuid, create_photo_prediction_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param create_photo_prediction_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CreatePhotoPrediction) with additional information such as HTTP status code, headers
    predictions_create_with_http_info = function(observation_uuid, create_photo_prediction_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }

      if (missing(`create_photo_prediction_request`)) {
        stop("Missing required parameter `create_photo_prediction_request`.")
      }



      if (!is.null(`create_photo_prediction_request`)) {
        local_var_body <- `create_photo_prediction_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/identification-tasks/{observation_uuid}/predictions/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "application/x-www-form-urlencoded", "multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CreatePhotoPrediction"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param photo_uuid 
    #' @param ... Other optional arguments
    #'
    #' @return void
    predictions_destroy = function(observation_uuid, photo_uuid, ...) {
      local_var_response <- self$predictions_destroy_with_http_info(observation_uuid, photo_uuid, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param photo_uuid 
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    predictions_destroy_with_http_info = function(observation_uuid, photo_uuid, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }

      if (missing(`photo_uuid`)) {
        stop("Missing required parameter `photo_uuid`.")
      }



      local_var_url_path <- "/identification-tasks/{observation_uuid}/predictions/{photo_uuid}/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`photo_uuid`)) {
        local_var_url_path <- gsub("\\{photo_uuid\\}", URLencode(as.character(`photo_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PaginatedPhotoPredictionList
    predictions_list = function(observation_uuid, page = NULL, page_size = NULL, data_file = NULL, ...) {
      local_var_response <- self$predictions_list_with_http_info(observation_uuid, page, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param page (optional) A page number within the paginated result set.
    #' @param page_size (optional) Number of results to return per page.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PaginatedPhotoPredictionList) with additional information such as HTTP status code, headers
    predictions_list_with_http_info = function(observation_uuid, page = NULL, page_size = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }




      query_params[["page"]] <- `page`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/identification-tasks/{observation_uuid}/predictions/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaginatedPhotoPredictionList"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param photo_uuid 
    #' @param patched_photo_prediction_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PhotoPrediction
    predictions_partial_update = function(observation_uuid, photo_uuid, patched_photo_prediction_request = NULL, data_file = NULL, ...) {
      local_var_response <- self$predictions_partial_update_with_http_info(observation_uuid, photo_uuid, patched_photo_prediction_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param photo_uuid 
    #' @param patched_photo_prediction_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PhotoPrediction) with additional information such as HTTP status code, headers
    predictions_partial_update_with_http_info = function(observation_uuid, photo_uuid, patched_photo_prediction_request = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }

      if (missing(`photo_uuid`)) {
        stop("Missing required parameter `photo_uuid`.")
      }




      if (!is.null(`patched_photo_prediction_request`)) {
        local_var_body <- `patched_photo_prediction_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/identification-tasks/{observation_uuid}/predictions/{photo_uuid}/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`photo_uuid`)) {
        local_var_url_path <- gsub("\\{photo_uuid\\}", URLencode(as.character(`photo_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "application/x-www-form-urlencoded", "multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PhotoPrediction"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param photo_uuid 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PhotoPrediction
    predictions_retrieve = function(observation_uuid, photo_uuid, data_file = NULL, ...) {
      local_var_response <- self$predictions_retrieve_with_http_info(observation_uuid, photo_uuid, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param photo_uuid 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PhotoPrediction) with additional information such as HTTP status code, headers
    predictions_retrieve_with_http_info = function(observation_uuid, photo_uuid, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }

      if (missing(`photo_uuid`)) {
        stop("Missing required parameter `photo_uuid`.")
      }



      local_var_url_path <- "/identification-tasks/{observation_uuid}/predictions/{photo_uuid}/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`photo_uuid`)) {
        local_var_url_path <- gsub("\\{photo_uuid\\}", URLencode(as.character(`photo_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PhotoPrediction"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param photo_uuid 
    #' @param photo_prediction_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PhotoPrediction
    predictions_update = function(observation_uuid, photo_uuid, photo_prediction_request, data_file = NULL, ...) {
      local_var_response <- self$predictions_update_with_http_info(observation_uuid, photo_uuid, photo_prediction_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid UUID of the Observation
    #' @param photo_uuid 
    #' @param photo_prediction_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PhotoPrediction) with additional information such as HTTP status code, headers
    predictions_update_with_http_info = function(observation_uuid, photo_uuid, photo_prediction_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }

      if (missing(`photo_uuid`)) {
        stop("Missing required parameter `photo_uuid`.")
      }

      if (missing(`photo_prediction_request`)) {
        stop("Missing required parameter `photo_prediction_request`.")
      }




      if (!is.null(`photo_prediction_request`)) {
        local_var_body <- `photo_prediction_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/identification-tasks/{observation_uuid}/predictions/{photo_uuid}/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`photo_uuid`)) {
        local_var_url_path <- gsub("\\{photo_uuid\\}", URLencode(as.character(`photo_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "application/x-www-form-urlencoded", "multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PhotoPrediction"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return IdentificationTask
    retrieve = function(observation_uuid, data_file = NULL, ...) {
      local_var_response <- self$retrieve_with_http_info(observation_uuid, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param observation_uuid 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (IdentificationTask) with additional information such as HTTP status code, headers
    retrieve_with_http_info = function(observation_uuid, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`observation_uuid`)) {
        stop("Missing required parameter `observation_uuid`.")
      }


      local_var_url_path <- "/identification-tasks/{observation_uuid}/"
      if (!missing(`observation_uuid`)) {
        local_var_url_path <- gsub("\\{observation_uuid\\}", URLencode(as.character(`observation_uuid`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # API key authentication
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "IdentificationTask"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
